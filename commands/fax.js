module.exports.help={
    name:'fax',
    aliases:[]
}

module.exports.run=async(client, message, args)=>{
    return message.channel.send("Daniel doesn't like Torian, and torian doesn't like daniel either")
}